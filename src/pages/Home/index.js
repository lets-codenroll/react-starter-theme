import "./style.scss"

const Home = () => (
  <div className="home">
    <h1>Welcome to the Home Page!</h1>
    <p>This is the main landing page of your application. Explore the site using the navigation bar above.</p>
  </div>
);

export default Home